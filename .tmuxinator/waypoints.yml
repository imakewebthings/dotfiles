# ~/.tmuxinator/waypoints.yml

name: waypoints
root: ~/projects/waypoints/

windows:
  - ide:
      layout: main-vertical
      panes:
        - vim
        - gitsh
        - npm run tdd
  - lrps:
      layout: horizontal
      panes:
        - http-server -p 8000
